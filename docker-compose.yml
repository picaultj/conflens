services:
  app:
    build:
      context: .
      args:
        # Use HOST_UID and HOST_GID as requested for volume permission handling
        - USER_UID=${HOST_UID:-1000}
        - USER_GID=${HOST_GID:-1000}
        # Proxy settings for build-time access (e.g. if you are behind a corporate proxy)
        - HTTP_PROXY=${HTTP_PROXY:-}
        - HTTPS_PROXY=${HTTPS_PROXY:-}
        - NO_PROXY=${NO_PROXY:-}
        - http_proxy=${http_proxy:-}
        - https_proxy=${https_proxy:-}
        - no_proxy=${no_proxy:-}
    image: conflens
    ports:
      - "6868:6868"
    # API keys / endpoints — copy .env.example to .env first.
    env_file:
      - path: .env
        required: false
    environment:
      - HTTP_PROXY=${HTTP_PROXY:-}
      - HTTPS_PROXY=${HTTPS_PROXY:-}
      - http_proxy=${http_proxy:-}
      - https_proxy=${https_proxy:-}
      - NO_PROXY=$NO_PROXY # to avoid communication issues
      - no_proxy=$no_proxy # to avoid communication issues
    volumes:
      # Persist scraped listings, abstracts and classification results.
      - cache:/home/app/.cache/conflens
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:6868/')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

volumes:
  cache:
