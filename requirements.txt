# Dependencies for the Hugging Face Gradio Space (sdk: gradio).
#
# HF installs these with pip, then runs space_app.py (which imports the conflens
# package from this repo). Deliberately excludes nicegui — the Space runs the
# Gradio GUI only. Keep in sync with the runtime deps in pyproject.toml
# (everything except nicegui) plus gradio.
anthropic>=0.40
openai>=1.40
litellm>=1.40
python-pptx>=0.6.21
python-dotenv>=1.0
gradio>=4.44
